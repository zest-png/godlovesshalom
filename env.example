# 目前範例未強制使用環境變數；保留檔案方便團隊後續擴充
# NODE_APP_PORT=3000
# PY_APP_PORT=8000

# Render / 其他雲端部署（前端 Static Site 需要知道 API 的完整網址）
# VITE_API_BASE_URL=https://xxx.onrender.com

# SQLite 資料檔放置位置（例如 Render 的 Persistent Disk 掛載到 /var/data）
# APP_DATA_DIR=/var/data


